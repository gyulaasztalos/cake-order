# --- Private planning docs — MUST NOT be published (this repo is public) ---
PLANNING.md
NOTES.md

# --- Gallery photos — NEVER in git: this repo is public, and in production
# --- the photos come from a mounted volume, not from the image (PLANNING §r3).
app/static/img/gallery/cakes/*
app/static/img/gallery/desserts/*
!app/static/img/gallery/cakes/.gitkeep
!app/static/img/gallery/desserts/.gitkeep

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage

# --- Env / secrets ---
.env
*.env
!*/config.env

# --- OS / editor ---
.DS_Store
*.swp
.idea/
.vscode/
